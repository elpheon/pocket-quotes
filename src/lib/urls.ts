// PRODUCTION: Update these URLs before submitting to app stores.
// These are the public URLs the app uses for its Privacy Policy and Support pages.
export const APP_DOMAIN = 'outtapocket.lovable.app';
export const PRIVACY_URL = `https://${APP_DOMAIN}/privacy`;
export const SUPPORT_URL = `https://${APP_DOMAIN}/support`;
